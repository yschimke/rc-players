package ee.schimke.composeai.rcplayer.protocol

/** Codecs for stream-structure operations (`ComponentStart` and `Skip`). */
internal val rcStructuralOperationCodecs: List<RcOperationCodec<out RcOperation>> = emptyList()
