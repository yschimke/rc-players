package ee.schimke.composeai.rcplayer.protocol

/** Codecs for referenced-operation storage, definitions, arguments, calls, and expansion. */
internal val rcMacroOperationCodecs: List<RcOperationCodec<out RcOperation>> = emptyList()
