package ee.schimke.composeai.rcplayer.protocol

/** Codecs for bitmap-font data, measurement, and draw operations. */
internal val rcBitmapFontOperationCodecs: List<RcOperationCodec<out RcOperation>> = emptyList()
