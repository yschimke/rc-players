# Native AppKit StateLayout transition

`before.png` and `after.png` are captures of the corpus's
`animation_state_transition` fixture immediately before and after its state-changing click. The
native AppKit player retains the outgoing StateLayout branch and uses its Core Animation transition
between these states.
